# External pilot

`track-existing.ts` is the minimum adoption path for another CKB builder: create one intent, register an already-broadcast transaction hash and wait for the project's configured confirmation policy. This is useful for validating the shared recovery need before asking a project to replace its broadcast path.
